# Implementación Bloque 4

- Reporte de servicios desde elementos que exponen `data-service-id`.
- Helper para reportar perfiles cuando el componente expone `data-user-id` o `data-provider-id`.
- Panel Admin con filtros y transición de estados.
- Mensaje de suspensión preparado para la respuesta `ACCOUNT_SUSPENDED` del backend.
- No se modifica el flujo de pagos.

Antes de fusionar/desplegar se debe comprobar la integración real con la estructura DOM actual y el runtime backend.
